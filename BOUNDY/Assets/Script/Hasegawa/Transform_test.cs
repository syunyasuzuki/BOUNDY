using UnityEngine;

public class Transform_test : MonoBehaviour
{

    SpringCon springcon = null;

    // Start is called before the first frame update
    void Start()
    {
        springcon = GetComponent<SpringCon>();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
